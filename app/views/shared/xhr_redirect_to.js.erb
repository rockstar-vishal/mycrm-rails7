$(document).ajaxStop(function() {
  $('input[type=submit]').attr('disabled', true);
});
validNavigation = true;
var redirectUrl;
redirectUrl = '<%= raw @args[:redirect_to] %>';
window.location.href = redirectUrl.length ? redirectUrl : window.location.href;
