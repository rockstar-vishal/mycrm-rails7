$("#spendModal").modal("hide");
$("#flash-messages").html('<div class="alert alert-success" role="alert"><%= j flash[:notice] %></div>');

setTimeout(() => {
  $("#flash-messages").fadeOut();
}, 5000);

setTimeout(() => {
  location.reload();
}, 1000);
